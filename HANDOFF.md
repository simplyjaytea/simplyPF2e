# HANDOFF.md — live session baton

Read this first, then CLAUDE.md. Historical audit and QA evidence is preserved in HISTORY.md.

## Current session — 2026-08-30

- Active implementation branch is now `codex/one-click-grounding`, based on the completed local PC stack (`adfb72c`). No GitHub/VPS mutation was performed; `.claude/` remains user-owned and untouched.
- The post-create transaction slice is complete locally but not published: Monster/NPC, Encounter, and PC creation now retain the exact in-flight item data, verify persisted `compendiumSource` identities (rather than ambiguous names), require every persisted spell to point to a real casting entry, and only commit after those checks. Converted NPC feat actions retain their original compendium source. Completion manifests now visibly count ordinary/focus casting entries as module-built infrastructure, including focus-only characters. Creation failures roll back only uncommitted documents; if cleanup fails, the draft is discarded and the surviving actor/folder is named so retry cannot duplicate it. Notification/sheet/result failures occur only after commit and cannot delete valid work. Local tests cover source-name collisions, spell-entry relationships, rollback failure, builder-reported stranded PCs, and presentation-safe commit ordering; expanded Monster/NPC/Encounter lifecycle mocks and live Foundry transaction QA remain open.
- Creature and encounter preflight now requires an enabled bestiary Actor pack before a provider request. The deterministic exact scaffold matcher favors shared traits, then size and level, with a level/size fallback for valid unfamiliar trait combinations. It still supplies token/prototype structure only; module table statistics and exact embedded content remain authoritative. Focused source-readiness and matcher regressions pass; live token/scaffold behavior remains on the VPS checklist.
- First one-click UI/transaction slice is local: `single` became distinct `monster` and `npc` intents (shared NPC engine); primary Generate creates after the existing generation/validation pipeline, while Preview Plan stays no-write. NPC intent is passed as generation guidance. Single-actor drafts clear before sheet presentation, and failed encounter writes best-effort delete every newly created actor and its new folder before preserving the retryable plan.
- This is not yet the complete approved roadmap: class-feature graph resolution, prerequisite-checked feats, reloadable/repeating-ammunition and investment-aware PC loadouts, deeper bestiary mechanics, and live Foundry acceptance remain. Do not claim full unattended PC completeness from this slice.
- Exact selection now covers spells, focus spells, equipment, loot, ancestry, background, class, heritage, and feats. Candidate lists carry an opaque ID plus private exact `{packId,_id}` reference; new model prompts select IDs, and resolvers retain that reference instead of re-fuzzy-matching a selected name. Focus spells reuse the existing grounded spell-selection request and revalidate the loaded spell's focus trait before embedding. Legacy name replies are accepted only for an ID-less migration/test catalog; a live catalog refuses ambiguous names.
- Final one-click resolution now explicitly uses retained exact references for PC ABCs, spells/focus spells, equipment, and loot, and for creature spells/focus spells/equipment/loot. The permissive name lookup remains only in legacy/pre-selection callers. Thus a failed grounded pass produces an unresolved manifest record rather than a post-selection fuzzy match. Canonical coin lines remain module-built. Scrolls select from a bounded, tradition-neutral exact spell slice in the existing loot request and retain that private source reference through module-built scroll creation; spell sources are therefore required even for a non-caster, before provider spend.
- Creature class feats now receive their own bounded exact selection pass (at most three, from level-capped `class` feats), then retain the selected source reference through creation. Feat sources now preflight for every intent, before provider spend. This closes the old creature feat name lookup, but does not claim full creature feat-prerequisite/actor-context validation.
- Creature special abilities now have the same explicit boundary: the selector receives bounded real bestiary action candidates and published choices clone that exact document. Any unlisted concept ability becomes a clearly labeled passive narrative entry, stripped of action economy, traits, and module-authored mechanics; no unselected draft becomes a mechanical action through name lookup. The concept prompt now requests narrative-only prose for bespoke abilities and ability sources preflight before provider spend. Bounded module templates for bespoke mechanical abilities remain an open follow-up.
- Exact references are now identity-validated: a final resolver accepts only the in-memory `{packId,_id}` object minted by this run's offered candidate catalog, not a lookalike object a model could fabricate. This applies to strict creature content and PC ABC/spell paths; permissive legacy/pre-selection lookup remains separate. The focused boundary regression proves both rejection of a plain reference and acceptance of an issued candidate.
- PC feat finalization now has the same strict boundary. A one-click feat selection must be the locally issued candidate for its exact slot; a fabricated/cross-slot reference cannot switch it to a duplicate-named document. If the model omits or invalidates a pick, the existing PC-entitlement fallback selects the first unused exact candidate already offered to that slot. Legacy/pre-selection calls retain their direct allowed-pack reference and name lookup behavior. The completion manifest blocks a slot only if no unused issued candidate remains. Independent review approved the strict/legacy split.
- NPC strikes now validate damage types, attack traits, and attack effects against PF2e's live configured choice catalogs before actor data is assembled. Valid `thrown-N` traits determine the same range the system will derive. Ranged strikes use PF2e 8.4.1's `system.range: { increment, max }` schema (5–500 feet), not the obsolete `range-increment-*` trait encoding. A production-path mock regression covers invalid-field rejection, range bounds, thrown reconciliation, and emitted Melee data. Independent schema review checked the fixed runtime damage catalog and exact PF2e 8.4.1 Melee sources; all 53 local regression files, script syntax checks, JSON parsing, and whitespace checks pass. Live sheet/range behavior remains on the VPS acceptance list.
- A transient completion manifest guards every generated creature, encounter member, and PC before the plan becomes creatable. Required unresolved spells, focus spells, feats, equipment, loot, and PC ABCs fail the plan; real coins and real-spell scrolls are labeled module-built, while unresolved custom mechanical placeholders no longer qualify. Custom creature ability prose is explicitly `custom-narrative` and non-required. The manifest is not persisted; after creation its count-only grounding summary and exact generation token report remain in the completion card.
- A known PC spell plan is now strict at the slot level: if exact grounding fails, its expected cantrip/ranked picks remain empty in the plan and each missing pick becomes a blocking manifest record. It never falls back to a first-draft spell name or fuzzy match. This prevents a spellcasting PC from being created with an incomplete published spell plan.
- Generator preflights the configured provider and required enabled compendium categories before it starts a billable request. The UI displays separate labeled **AI Provider** and **Compendium Content** readiness rows with effective pack count and a direct Sources button; the old duplicate unlabeled sources gear in the Generate row was removed. Required categories vary by mode/spellcasting setting; missing source categories block before generation.
- Uniform one-click completion card added: after Monster/NPC/PC creation or atomic encounter creation, the non-retryable result remains in the generator with created name/count, count-only grounding summary, token details, **Open Sheet** (first encounter actor), and **Generate Another**. The successful draft always clears before it. PC native-choice/skill review remains alongside this card. A generation failure now retains its real error rather than attempting to reference a nonexistent actor.
- Generator form hierarchy is now prompt → prominent level → native **Advanced options** disclosure → actions. Preset selection, rarity, treasure, spellcasting, party size, and threat live in Advanced. The generation surface has only a labeled **Manage Presets** control; custom preset creation/editing/duplicate/delete/export/import live in that dedicated dialog, which now has **New Preset**. Form state is read before opening the manager so its refresh cannot discard current input.
- Isolated local release-gate branch `codex/release-json-validation` contains `7e00930` (`ci: validate release manifests`), based on `origin/main`. It adds `jq empty module.json lang/en.json` to the reusable release verifier. It has not been pushed, opened as a PR, or run on GitHub; live workflow verification is still a release gate.
- Complete-only PC class gate currently offers Fighter only. PF2e 8.4.1 source confirms Rogue's Racket, Investigator Methodology, Arcane School, and Arcane Thesis are mandatory native class-grant choices re-fetched by the ABC pipeline; they cannot honestly be presented as unattended until a staged grant-graph resolver exists. If Fighter is unavailable, PC generation stops before any provider request. Other classes are intentionally not approximated in the complete path.
- Ability boosts now take optional concept `abilityPriorities` enum slugs, validated locally/deduplicated. The class key ability remains forced first; preferences determine the remaining free actor/ABC boost ordering rather than a fixed Constitution-first array. The model still supplies no scores, ranks, counts, or math.
- Bestiary Actor source category/scaffold added. Configured/default bestiary Actor packs are detected in Sources; generation deterministically selects the closest real NPC actor by traits/size/level, reuses its art and safe prototype-token structure, but never its items/statistics. Actor scaffold selection has direct tests. Live PF2e token behavior still needs VPS QA.
- New local PC loadout slice: after native ABC creation has prepared the actual actor proficiencies, the builder matches only generation-local embedded IDs (not a merely shared compendium UUID) and readies one proficient armor plus up to two hands of proficient weapons/shields/tools. It mirrors PF2e 8.4.1's category/group/base/synthetic proficiency selection and keeps untrained or conflicting gear stowed with a visible review warning. Compatible generated ammunition is linked via PF2e's own `isAmmoFor` check for non-repeating reload-0 weapons; reloadable/repeating weapons still require native subitem loading. The write runs inside the existing rollback boundary. Investment, containers, and activation requirements remain native-sheet work. New pure regressions cover source-collision isolation, category/group/base/synthetic ranks, armor/hand conflicts, automatic selected ammo, manual reloadable/repeating ammo, and native-data failure. All 56 local regression files, syntax, JSON, and whitespace checks pass. Live loadout behavior remains on the VPS checklist.
- Free Archetype variant feats now retain their `class` category for compendium filtering but use PF2e 8.4.1's separate `archetype-<level>` location group, so they cannot collide with ordinary `class-<level>` slots. A source-backed pure slot test and production creation-path mock cover the distinction; independent schema review approved it. This corrects placement only: comprehensive feat-prerequisite validation remains open, so it does not widen the complete PC-support claim.

- Latest user request: continue the approved One-Click, Compendium-Grounded roadmap. Item Forge remains deferred.
- Working branch: `codex/one-click-grounding`, based on the completed local PC stack. It is not published; `origin/main` was `004bffc` (PR #82 / release v0.3.5.42) at last API check. Check git for authoritative current state.
- No authenticated GitHub writes, push, PR, merge, or deployment in these local sessions. User permission to push/open a PR was requested and has not been granted. Branch + PR only; never direct main.
- User-owned untracked `.claude/` is untouched.

## Completed locally — awaiting publication permission

- `6f188d0`: new-PC current HP finalized from PF2e's derived max after awaited native item creation; existing Int-language expansion moved into the same rollback boundary. No module HP math or max/temp writes.
- `be5859d`: grounded choice automation: final cloned item sources are collected before Actor.create. Forced/key-ability/unambiguous concept choices resolve locally; other supported choices use a single optional callback batch. Generator supplies the configured AI provider. Exact opaque group/option IDs are validated and mapped locally to real values; first-option guessing is removed.
- Bounds: 24 groups, 32 options/group, 512 total options. Excess/unsupported/unanswered choices stay native, not truncated or guessed. Existing authored selections/preselect records are untouched. Only direct ChoiceSets and one-level GrantItem choices are reachable.
- UI separates AI selection progress from native feature application and tells GMs to answer remaining dialogs. Failed selection falls back to native input with a warning. Shared JSON request errors now retain spent token usage across failed attempts.
- Independent Sol review approved Terra/root implementation with no remaining actionable findings after fixing incomplete-catalog coercion and lost failure usage.
- Verification: 37 regression files pass; changed-module syntax, module/localization JSON parsing, and patch whitespace checks pass. Source checked against PF2e master and 8.4.1. No new live Foundry verification.

## Base spellcasting work — `b72c63d`

- Seven source-qualified Remaster profiles: Bard, Cleric, Druid, Oracle, Sorcerer, Witch, Wizard. Exact publication title + remaster flag and class identity gate the profile. Slots are base-only; Sorcerer/Oracle now use their published 3/4 progression, other supported profiles 2/3. Sol independently compared all 140 level rows against the real PF2e 8.4.1 Classes journal. Native class `spellcasting` is proficiency, not a slot table.
- Prepared classes create an explicit daily plan, including cantrips, using real embedded spell IDs and native prepared arrays. Repeated ranked picks may share a document across ranks/slots. Spontaneous spells retain assigned heightening and deduplicate by real document UUID + rank. Builder rechecks source type/traits/tradition/ritual/base rank/caps, derives profile from the grounded class, clears stale locations, and keeps known empty entries for manual completion.
- Existing selectSpells call now supports a PC plan: the AI selects exact names and rank enum slugs only; the module maps ranks and supplies counts. No extra AI stage. Candidate sampling reserves enough choices per rank inside the existing 96-candidate cap. New deterministic task cap 3072 tokens (configured ceiling and bounded retry still apply).
- Preview warns about base-only coverage, empty slots, signature spells, restricted bonus slots/spells, variable bloodline/patron traditions, and generic legacy/unsupported profiles. Failed/empty known plans keep empty slots instead of substituting an unplanned draft. No subclass, signature, spellbook/familiar inventory, font/curriculum, complex/mixed caster or feat-bonus automation is claimed.
- All 40 regression files, all module syntax checks, module/localization JSON parsing, and patch whitespace checks pass. Production tests cover provider request/enum validation and native document payloads; UI wiring is static-tested, not live Foundry-tested. No real provider calls in this pass.
- Final independent Sol review approved the base-profile diff and expanded tests with no remaining actionable findings. This is not approval of full subclass/signature automation or proof of live casting behavior.

## Latest local follow-up — ordinary signatures and repertoire capacity

- Still on `codex/pc-spellcasting`, after `b72c63d`. `pcSpellPlan` separates native slots, preparation/repertoire picks, and eligible signature ranks. Recognized Remaster Bard/Oracle/Sorcerer gain ordinary signature choices at level 3; one per learned positive rank, including ten. At 19–20 they learn two common rank-10 spells but have one slot. Both facts were fetched from real master and PF2e 8.4.1 feature/source files.
- Reused the same PC spell-selection request: `signature`/`regular` enum markers become internal booleans after exact-name/rank/count validation. Conflicting markers at one rank all stay regular. Candidate sampling preserves enough common ranked choices for rank ten without raising the cap; lower-base common spells may be learned heightened. The builder separately checks actual common rarity, source identity, profile eligibility, and marker collisions before setting native `location.signature`. Stale flags do not survive cloning. No hand-authored REs or runtime casting overrides.
- Preview labels planned signatures and reports selected/eligible counts; missing or rejected choices remain manual. Prepared and unsupported/legacy profiles do not gain signatures. Bonus-signature feats and subclass-dependent casting remain outside this ordinary-class implementation.
- All 41 regression files, all `.mjs` syntax, module/localization JSON, and diff whitespace checks pass. Sol independently approved the final diff and targeted tests. No provider or live Foundry calls; actual virtual signature rows, upward/downward casting, and slot expenditure need VPS QA.

## Completed current feature — native-choice review

- Added a read-only post-creation snapshot of embedded ChoiceSets lacking a recorded selection, including native ABC-generated features. Optional choices and suppressed feats are excluded; ignored/predicated rules are review hints, not proof of a required missing choice. Malformed/unreadable items produce an incomplete-inspection warning. No predicate evaluation, rule reapplication, actor mutation, or deletion.
- Generator retains an escaped report for the last created PC until dismissed, replaced by another successful PC creation, or the app closes. Open Character targets that exact actor. Copy explicitly says the snapshot does not refresh after edits and is not full validation; no empty “all complete” report is shown.
- Fixed PC duplicate-retry risk: the draft clears as soon as native creation succeeds, before report/notification/sheet presentation. Failed sheet or final report rendering warns that the actor exists; genuine creation failure retains the draft.
- Fresh PF2e master and 8.4.1 source checks confirm there is no supported general ABC preselection seam: ABC children are fetched/cloned inside native creation. Existing direct/one-level GrantItem preselection remains intact. Native flags are selection outputs, not a generic child-choice input.
- Independent reviewer approved the final diff after its report-render failure finding was fixed. All 43 regression files pass, including pure review and unmodified-production-app lifecycle tests with mocked imports/platform. All module syntax, JSON parsing, and whitespace checks pass. Template rendering itself and live native behavior still require VPS QA; no real provider or Foundry requests in this pass.

## Exact next steps

### Character Skill Completion — finished locally, not live-verified

- New pure `pc-skills.mjs` owns the core catalog, validated priority ordering, exact class schedule validation, and chronological allocation/reconciliation. AI adds only optional `skillPriorities` enum slugs in its existing concept request. Missing/invalid preferences use the explicitly labeled key-ability default; no extra provider request, setting, or arbitrary Lore creation.
- Training uses `max(0, class additional + native Int base)` plus independently counted direct class/background duplicate replacements. Native `.base` is pre-RE integer Intelligence and can include apex. A detached level-1 ABC/heritage-only clone verifies initial Int; later additional training is available only at the creation level, conservatively, not retroactively. Skill schedules come from real `skillIncreaseLevels.value`; missing/malformed schedules warn and receive no fallback.
- Provisional ranks (including fixed ABC training) are seeded before native preCreate so skill-predicate roll options remain valid. Purchased ranks and seeded native floors have separate ownership. After real item/grant creation, detached clones clear only still-owned sources to observe native floors, then project reconciliation. Native source writes and higher ranks survive; non-floor/injected transforms and uncertain timing produce warnings. Retained increases keep their prerequisite chains, and replacement training cannot consume a pinned increase. Skill/Lore writes happen before final HP/language finalization inside the existing rollback boundary.
- Internal `createCharacterActor` now returns `{actor, skillReport}`; its generator caller is updated. No public module API/settings or persisted schema changes. Report data is passed in memory only. Preview shows priorities; the post-create snapshot shows actual ranks and unsupported/unspent cases. Presentation failures still cannot leave a duplicate-creation draft.
- Independent schema/balance reviewer approved after catching and retesting refund-chain, late-Int, seed ownership, injected-path, replacement-collision, and negative-Int replacement bugs. Reviewer also checked 30,000 deterministic generated reconciliations for exact +1 events, rank monotonicity, and budget accounting.
- Final verification: all 46 regression files pass, every `scripts/*.mjs` passes `node --check`, `module.json` and `lang/en.json` parse, and `git diff --check` passes. Older actor mocks intentionally exercise the missing-native-clone warning path; the new production-builder tests cover clone-based allocation. No live Foundry or real provider request was made.
- Conservative limits: no historical reconstruction of arbitrary native grants; no inferred feat/subclass training allowances or duplicate native-feat replacements; no comprehensive feat-prerequisite validation. Later Int timing is intentionally under-assumed. Background Lore uses real embedded IDs rather than name-derived skill paths. Detached clones/native grant execution remain unverified in real Foundry.
- Live acceptance after authorized publication: level-1 Fighter; high-Int Wizard; negative-Int character; Ranger/background duplicate Survival; level-2/7/15/20 Rogue or Investigator versus ordinary class cadence; a skill-dependent native ChoiceSet; Skilled Human and overlapping native skill grants; existing background Lore; skill summary/dismiss/open actions; final full HP and no duplicate after sheet/report failure. Include the preceding HP/choice/spell/signature QA below.

### Publication and broader follow-up

1. Check git for the local HP, choice automation, base spellcasting, signature/repertoire, native-review, and skill-completion commits. Obtain user permission before pushing/opening a PR; implementation approval is not publication approval.
2. PR/live QA checklist after release: create a level-1 Dwarf Fighter and a higher-level/high-Int PC; inspect grounded skill/feature selections, native grant chains, bonus languages, and current HP equal to final max.
3. Verify the UI transitions from AI selection to native feature application; complete remaining dialogs. Exercise a failing provider in a disposable test context and confirm native fallback, spent token reporting, and no duplicate/partial actor.
4. After publication, exercise the new review report: complete a normal PC, dismiss a native feature prompt, inspect a conditional/optional/suppressed feature, open the exact created actor, and dismiss the report. Confirm the warning is a snapshot, not a required-choice count or a live validator. ABC-generated/deeper/dynamic/predicated/optional/drop-enabled choices still stay native; never delete actors merely for dismissed prompts.
5. Native HP caveat: Item._onCreate may launch detached actor updates (e.g. LoseHitPoints). The HP fix follows awaited item creation, not every rule/module follow-up; test unusual HP-mutating rules if present. No delays or hook overrides were added.
6. Release-workflow `lang/en.json` validation remains a separate open gap. Focus spells, Free Archetype, PC runes, presets, and a full local-provider actor flow also need targeted QA.
7. Spellcasting live QA after publication: create a level-1/5 Wizard or Cleric and level-2/3/19 Bard, Sorcerer, or Oracle; check base counts, five cantrips, prepared-slot IDs/casting/expending, repeated and heightened spells, entry ability/tradition, and unchanged native grants/full HP. At 3+ inspect signature virtual rows, cast a learned-heightened signature downward and upward, and confirm correct slot expenditure; at 19 verify two common rank-ten repertoire spells and one slot. Exercise an empty/failed plan and missing-signature preview; verify disabled casting stays disabled. Restricted bonus slots and bloodline/patron choices still need manual inspection.
8. Continue automation by grounding subclass-dependent casting and bonus signature feats against the installed system; unsupported Magus/Summoner/Psychic/Animist/legacy casting still needs verified, distinct handling. Do not extrapolate the seven-profile table; PF2e master and the installed 8.4.1 packs differ materially for some classes.

## Prior live QA / environment

- Test world: https://foundry-test.gigaserver.xyz/game (Foundry 14.365 / PF2e 8.4.1 at last recorded QA, not rechecked this session).
- Previous agents verified native grants on v0.3.5.34 and full cloud creature/PC creation, heightened NPC spells, equipment-value deduction, responsive UI, and one passive/activated forged item on v0.3.5.39. This does not verify v0.3.5.42's changes or this branch.
- QA artifacts previously left: "Stormcaller Shaman", "Bryndra Ironveil", "Stormwarden's Amulet" + macro, plus earlier actors. Do not delete without user approval.
