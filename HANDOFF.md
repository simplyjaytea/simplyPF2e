# HANDOFF.md — live session baton

Read this first, then CLAUDE.md. Historical audit and QA evidence is preserved in HISTORY.md.

## Current continuation — post-release v0.3.5.61 Actions runtime maintenance

- User authorized push and merge. Commit `f9b59d0` was pushed as `chore/github-actions-node24`; PR **#101** passed Pull Request Checks run `33944344962` and merged to `main` as `e03c4a1`.
- Every `actions/checkout` and `actions/setup-node` use across all three workflows now uses v5, addressing the deprecated Node 20 action-runtime annotation while retaining Node 22 for project checks. Fetch depth, permissions, triggers, reusable-workflow wiring, release logic, and project runtime are unchanged.
- Local verification passed all **67 regression files**, **102 script syntax checks**, Node-based module/localization JSON parsing, and `git diff --check`. Independent reviewer `640d1683-e045-4f31-96d5-2e3f4322463f` approved with no findings. `jq`, `actionlint`, Ruby, and PyYAML were unavailable locally, but the upgraded actions then executed successfully on GitHub-hosted CI.
- Auto Release run `33944387562` completed successfully using the v5 actions and published **v0.3.5.61** with `module.json` and `module.zip`. No provider spend or Foundry mutation occurred, and the installed module was not updated. Current local branch `docs/actions-runtime-release` records this post-release state only; do not push it solely to trigger another release.

## Prior continuation — post-release v0.3.5.60 creature-feat QA

- **Publication and review blockers superseded:** while the parent session was paused, the user merged PR #100, making `origin/main` `5158902` and publishing **v0.3.5.60**. A fresh independent reviewer tool approved with no findings. A separate read-only reviewer inspected HEAD and adjacent validation but could not execute tests or mechanically diff, so that inspection is not execution evidence. BrowserOS verified successful PR checks run `33941974897`, successful Auto Release run `33941979908`, and release assets `module.json` and `module.zip`.
- This documentation branch, `docs/creature-feat-release-qa`, was created from `origin/main` `5158902`; it contains documentation only. The earlier sign-in/authentication, pending-review, and publication blockers remain below as preserved history but no longer block this handoff.
- BrowserOS updated **only SimplyPF2e**, from **0.3.5.59** to **0.3.5.60**, using Setup's module-specific update, then launched the `test` world and joined as GM. SimplyPF2e **0.3.5.60** loaded on PF2e **8.5.0**; no core or system update occurred.
- The actual Generator UI level-4 NPC Preview Plan produced **Goblin Scroll Courier**, with **19/19** compendium matches and one visible `Route Memory — NARRATIVE ONLY` ability. Loot displayed **Scroll of Fear (Rank 2)** correctly. Preview cost: **25,867 tokens**.
- That Preview Plan did not request creature-feat selection, so it is not end-to-end omission-bearing provider/UI acceptance. A separate small direct call to loaded production `selectCreatureFeats`, using a level-4 untrained-courier/Fleet wishlist, the actual 16 class candidates, and the real configured provider, returned `feats: []`, `omitted: true`, and usage `{ prompt: 1444, completion: 9, total: 1453, estimated: false }`. This live-checks the selector flag; the caller's empty-selection behavior is covered by the production-class local regression, not by a full omission-bearing provider UI run.
- Total QA spend was **27,320 tokens**. Actor count remained **19**; world items and macros remained **5** and **5** before/after the preview, and the direct selector also observed 19 actors. No Create Actor click occurred and no QA artifacts were removed. Task-owned BrowserOS page **17** retains the preview.
- A brief temporary fetch observer in the owned tab captured only the feat response content, forwarded responses unchanged, then restored the original `fetch` and deleted the observer. The preview also showed extra occult spells and equipment/loot despite a minimal/no-spellcasting prompt; this was not investigated. Treat it as a separate fidelity observation that advanced controls may influence, not as a release regression or proven cause.
- The earlier raw failure responses remain unavailable; this QA does not claim to prove both historical error causes. Parent's prior local validation remains the record for all **67 regression files**, **102 syntax checks**, module/localization JSON parsing, and whitespace gates.

## Prior continuation — explicit creature-feat omission (superseded)

- **Publication attempt after user authorization:** user requested “merge, update, and qa.” Commit `5f4dc2a` and this branch were pushed to origin. BrowserOS release tab 13 opened the GitHub comparison, but GitHub is signed out, so no PR or merge was performed. Sign in to GitHub in BrowserOS to continue. Independent review remains unavailable/pending; do not silently treat CI as that review. Release/update/QA have not run.
- Branch `fix/creature-feat-omission`, based on `3b96d7c` and carrying forward the post-v0.3.5.59 QA documentation. BrowserOS is connected; task-owned page 12 holds the test world. Loaded SimplyPF2e remains **0.3.5.59**, PF2e **8.5.0**, actor count **19**. No provider request, world-document write, deployment, push, PR, or merge occurred.
- Read-only live catalog checks: level-4 class candidates offer **Sneak Attacker** (class, level 4); **Fleet** is general, level 1, so correctly absent from that class-only catalog. Do not widen categories speculatively. Earlier failing runs' raw provider replies were not captured, so their precise cause is still unproven.
- Reproduced a production-caller contract defect locally: `selectCreatureFeats` explicitly permits `featIds: []`, but `#refineCreatureFeats` kept the unresolved draft when decoded feats were empty. The selector now returns a locally derived `omitted` flag only for an explicitly empty schema-valid reply, and the caller honors it. Nonempty all-invalid picks, absent catalogs, malformed replies, and provider errors still retain unresolved requirements; cancellation still propagates. Existing exact-reference identity, class/level eligibility, mixed-valid-pick behavior, and PC feat handling are unchanged.
- Added a production-class regression (test-only private-method renaming); observed failure before the caller fix and passing afterward. Expanded actual provider-path selector tests for explicit omission, invalid picks, malformed bounded retries, and usage accounting. All **67 regression files**, **102 script syntax checks**, module/localization JSON parsing, and whitespace gates pass. Test log: `/tmp/simplypf2e-qa/creature-feat-tests.log` in the local bash environment.
- Independent review is **not performed**: `pi-subagents` was removed and this session exposes no child-agent tool. Do not call this release-ready or bypass the repository's independent review bar. No schema, Rule Element, benchmark, or candidate-category change was made.
- Next: obtain an independent review of the optional-feat contract change, then request publication authorization for branch/PR/release. After authorized release/update, run one bounded real preview to capture selector outcomes and complete rendered narrative-count acceptance. The deterministic defect fix does not prove the prior two live errors are resolved.

## Prior continuation — blank scroll candidates and narrative counts

- Branch `fix/blank-scroll-candidates`, carrying forward the post-v0.3.5.58 QA documentation. Worker commit `27c5515` filters blank scroll consumables from ordinary equipment/loot candidate catalogs using indexed `system.category` and `system.spell`. Underlying template lookup remains available to the exact spell-grounded scroll builder; complete embedded-spell scrolls and other consumables stay eligible.
- Parent verified the pre-fix live module actually offered blank rank-1/2 equipment scrolls and rank-1/2/3 loot scrolls. Real PF2e 8.5.0 documents had category `scroll`, type `consumable`, and `spell: null`. Source proof: fetched PF2e 8.4.1 `src/module/item/consumable/data.ts` declares `spell: SpellSource | null`; master `packs/equipment/scroll-of-1st-rank-spell.json` has no embedded spell and instructs creation by dragging a spell.
- The courier's 12/13 display was twelve real matches plus a labeled narrative-only ability, not a demonstrated unresolved required item. Narrative-only rows are now excluded from the published-content match denominator without hiding the narrative itself or unresolved non-narrative items.
- Independent review `178db8b0-255b-4548-8d7f-5ba19a410a8d` caught the worker dropping the narrative flag at the caller. Parent fixed that seam and replaced the copied-helper test with actual production-class execution using test-only private-seam renaming. The test reproduced 12/13 before the caller fix and verifies 12/12 afterward; unresolved real ability/equipment still count as misses.
- All 66 regression files, all 101 script syntax checks, module/localization JSON parsing, and whitespace gates pass. Follow-up reviewer `d1945a5b-0faa-48e4-ae7d-870a86d4530b` approved the final correction with no findings. PR #99 merged at `d6775ad`; CI `33940839944` and Auto Release `33940860488` passed, publishing v0.3.5.59. BrowserOS updated only SimplyPF2e via Setup and relaunched the test world; v0.3.5.59 is loaded.
- Post-update live candidate API checks pass: no blank scroll templates in equipment or loot catalogs; real scroll-related equipment remains, and `findEntry` still resolves `Scroll of 1st-rank Spell` for internal assembly. Two bounded courier previews failed closed on unrelated unresolved feats (`Sneak Attacker`, then `Fleet` despite a no-feats prompt), reporting 22,357 and 26,994 tokens respectively. Actor count stayed 19 in both cases. No world documents changed. The catalog boundary is live-verified; final rendered narrative-count acceptance remains blocked by feat grounding, not claimed passed. Owned BrowserOS page 10 retains the second error.
- Current local branch `docs/blank-scroll-release-qa` records post-release QA; preserve for the next useful PR to avoid a documentation-only auto-release.

## Prior continuation — scroll preview labels

- Checkout: `C:\Users\jtf\Documents\Projects\simplyPF2e`, branch `fix/scroll-preview-label`, based on `f1d1eb5` (PR #97 / v0.3.5.57). The prior entry's pending PR/publication steps are superseded: #97 already merged.
- BrowserOS live QA found v0.3.5.57 active on Foundry 14.365 / PF2e 8.5.0. The actual Items-directory button appears once, opens Item Forge, and repeated clicks leave exactly one forge window. Switching Actors → Items retained one button. No module update was needed; existing QA artifacts were untouched.
- Fixed preview-only `Scroll of undefined`: exact scroll references contain identity, not `name`. The shared gear mapper now rebuilds scroll names only for named entries; ref-only entries retain the selector's already-grounded loot label. Creation and reference validation remain unchanged. The closing parenthesis was already correct.
- Added production-app regression covering ref-only scroll labels, stack suffixes, and legacy named entries. Observed the undefined-label failure before the fix. All 65 regression files, all 100 script syntax checks, module/localization JSON parsing, and whitespace checks passed afterward.
- Independent reviewer `6217c769-98b3-4347-a584-38bb8b60aa79` approved with no findings. Commit `1f640d5` merged as PR #98 / `8d777f5`; CI run `33939773183` and Auto Release `33939802058` succeeded, publishing v0.3.5.58 with both module assets. Installed v0.3.5.58 and post-update scroll-preview QA are now verified below. User authorized those operations; preserve branch + PR release discipline.
- Foundry initially required administrator authentication. After the user continued, Setup showed v0.3.5.58 already installed; no reinstall was performed. Launched the `test` world, joined as GM, and confirmed v0.3.5.58 loaded on Foundry 14.365 / PF2e 8.5.0.
- Post-release live scroll-label QA PASSED through the actual Generator UI and provider: level-4 goblin scroll-courier Preview Plan displayed `Scroll of Fear (Rank 2)` and `Scroll of Invisibility (Rank 2)` with no undefined label. The run reported 21,529 tokens. Preview only; Create Actor was not clicked and existing QA artifacts were preserved. BrowserOS page 6 retains the preview for inspection.
- Separate observations: the provider expanded gear/loot beyond the requested scroll and coins, and the preview reported 12/13 compendium matches. Generic scroll-template equipment also appeared. This acceptance verifies displayed scroll labels, not complete actor creation or resolution of these separate content issues.
- Current local branch is `docs/scroll-preview-release-qa`, based on released `8d777f5`, holding this post-release handoff. Keep these documentation updates for the next useful PR rather than merging solely to trigger another release.
- Armor activation wording is model-generated description, not a runed-item activation path. No prose-stripping or mechanics change is included.

## Prior session — 2026-09-05

- Public source is `origin/main` `220741b` (PR **#96**) / **v0.3.5.56**. The single audit-fix PR merged and its auto-release completed successfully.
- Active branch: `codex/item-forge-entry`, based on `origin/main` and carrying forward the two post-release documentation commits from the merged audit branch. It is the single intended next release branch; do not push or merge `main` directly.
- Completed audit fixes on this branch:
  1. Cancellation is owned by each `SpfApp` run. `_beginProgress()` returns its run signal, and all generation/forge provider calls carry that signal through `requestJSON`/`requestCompletion`; the old module-global signal setter is removed. Connection and model probes remain independent.
  2. Prewritten generated macro commands include a fixed runtime HTML escape helper. Acting, target, and healing actor names are escaped immediately before chat content or roll flavor; pre-escaped item/effect/duration values remain unchanged.
  3. Runed-item assembly now returns source data plus transient `{ priceGp, level }` preview data. `Item.create()` preserves cloned base `system.price` and `system.level`; PF2e preparation derives runed totals, which the module does not overwrite. The catalog excludes PF2e-specific weapons/armor, so this ordinary-base preview contract is not offered an incompatible specific item.
  4. The reusable release workflow now validates `module.json` and `lang/en.json` with `jq empty` before syntax/regression gates, covering auto-release, tags, and manual runs.
- New regressions cover two concurrent cancellation signals, hostile runtime actor names in chat/flavor, and preserved runed base source fields with correct preview estimates.
- Local verification passed: all **65** `scripts/*.test.mjs`, syntax checks for all **100** `scripts/*.mjs`, `jq empty module.json lang/en.json`, and `git diff --check`. A fresh Sol/High review returned `ship` with no findings; PR #96 checks and Auto Release run `33896508187` passed, including the new release JSON gate, and published both assets.
- Post-release live QA passed on **Foundry 14.365 / PF2e 8.5.0** with SimplyPF2e **0.3.5.56** loaded (the code/source review target remains PF2e 8.4.1):
  1. Concurrent Generator + Item Forge cancellation was exercised in both directions. Cancelling Generator left Forge running to a completed preview; cancelling Forge returned only Forge to its enabled state while Generator advanced to its own terminal result. Console output contained the expected Generator cancellation warning and no cross-request abort. The surviving Generator later failed closed on unresolved `Healing Potion (Minor)`, which is independent validation behavior.
  2. Runed preview/prepared-sheet parity passed: `+1 Ghost Touch Longsword` was level 4 / 110 gp in both preview and sheet; `+1 Slick Chain Mail` was level 5 / 205 gp in both. The armor flavor text said "Custom activation runes" despite a no-activation prompt, but no activation or extra rule was created.
  3. Generated activation macro escaping passed with actor `QA <b>Actor</b>`. The healing roll flavor preserved the literal markup text, and a condition-only macro produced `applied frightened 1 for 1 round to QA <b>Actor</b>.` with literal angle brackets while the condition affected derived stats.
- Pending Item Forge entry change: `scripts/simplypf2e.mjs` now shares the directory-button renderer and registers a GM/PF2e-only `renderItemDirectory` hook. It adds one localized **Item Forge** button, prevents duplicates on repeated renders, and calls the existing protected `openItemForge()` singleton. The permission regression covers player/non-PF2e invisibility, GM visibility, idempotence, and click behavior. README no longer describes the forge UI as hidden.
- Verification for the entry change passed locally: all **65** regression files, syntax checks for all **100** scripts, `jq empty module.json lang/en.json`, and `git diff --check`. Independent review, commit/push/PR/merge, auto-release, module update, and a real post-update Items-menu click remain pending.
- QA artifacts intentionally remain for inspection: actors `QA <b>Actor</b>` and `QA Caster`, their scene tokens, two `Clockwork Moth Scout` actors created during isolation runs, forged world items and companion macros, the `QA Open Item Forge` macro, the `QA Prepare Markup Actor` cleanup macro, and chat evidence. The GM character assignment and token targets were restored. One first-pass QA cleanup command called an unavailable Foundry API and logged an error before changing state; the corrected cleanup completed. Programmatic token targeting also exposed Foundry/PF2e `_drawTargetArrows` `clear` errors while still updating the target; this did not originate in SimplyPF2e.
- The untracked `.claude/` directory is user-owned and untouched.

## Next step

Preserve this post-release documentation on local branch `docs/actions-runtime-release` for the next useful change; do not push it solely to trigger another release. Updating SimplyPF2e from v0.3.5.60 to v0.3.5.61 in Foundry is optional and requires user direction; this release changes CI workflows and documentation only. Preserve BrowserOS page 17 and existing QA artifacts; do not perform another provider retry or Foundry mutation.

Previously: Obtain a fresh independent review of the Item Forge entry diff, commit and push `codex/item-forge-entry`, open and merge its PR, wait for the single auto-release, update SimplyPF2e in Foundry, and verify that the Items-directory button appears once and opens Item Forge. Preserve the successful QA artifacts until the user explicitly approves deletion. Afterward, triage the non-blocking `Scroll of undefined (Rank 2)` preview and no-activation armor flavor observations. Also note the non-blocking GitHub annotation that `actions/checkout@v4` and `actions/setup-node@v4` still target deprecated Node 20 internally and were forced onto Node 24 by the runner.
